package main

import "fmt"

func anotherFor() {
	// var slice = []string{"alice", "bob", "mario"}

	// for i, v := range slice {
	// 	fmt.Println(i, v)
	// }

	// var mapper = map[string]int{"Alice": 90, "Bob": 10}
	type struct 

	for key, value := range mapper {
		fmt.Printf("%v, %d\n", key, value)
	}
}
